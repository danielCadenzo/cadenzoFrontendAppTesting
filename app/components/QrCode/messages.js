/*
 * QrCode Messages
 *
 * This contains all the text for the QrCode component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.QrCode';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the QrCode component!',
  },
});
