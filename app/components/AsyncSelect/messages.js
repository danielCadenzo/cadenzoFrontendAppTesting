/*
 * AsyncSelect Messages
 *
 * This contains all the text for the AsyncSelect component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AsyncSelect';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AsyncSelect component!',
  },
});
