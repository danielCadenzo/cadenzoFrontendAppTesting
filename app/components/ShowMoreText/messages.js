/*
 * ShowMoreTextJs Messages
 *
 * This contains all the text for the ShowMoreTextJs component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ShowMoreTextJs';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ShowMoreTextJs component!',
  },
});
