/*
 * TabRouter Messages
 *
 * This contains all the text for the TabRouter component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TabRouter';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TabRouter component!',
  },
});
