/*
 * Mapping Messages
 *
 * This contains all the text for the Mapping component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Mapping';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Mapping component!',
  },
});
