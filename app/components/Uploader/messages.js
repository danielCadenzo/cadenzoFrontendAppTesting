/*
 * Uploader Messages
 *
 * This contains all the text for the Uploader component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Uploader';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Uploader component!',
  },
});
