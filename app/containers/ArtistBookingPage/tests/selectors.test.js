// import { selectArtistBookingPageDomain } from '../selectors';

describe('selectArtistBookingPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
