/*
 * SupportPage Messages
 *
 * This contains all the text for the SupportPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SupportPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SupportPage container!',
  },
});
