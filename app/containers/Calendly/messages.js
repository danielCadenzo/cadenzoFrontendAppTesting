/*
 * Calendly Messages
 *
 * This contains all the text for the Calendly container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Calendly';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "'s Booking Calendar",
  },
});
