/*
 * 404Page Messages
 *
 * This contains all the text for the 404Page container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.404Page';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the 404Page container!',
  },
});
