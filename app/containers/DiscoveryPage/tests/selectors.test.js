// import { selectDiscoveryPageDomain } from '../selectors';

describe('selectDiscoveryPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
