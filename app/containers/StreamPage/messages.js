/*
 * StreamPage Messages
 *
 * This contains all the text for the StreamPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.StreamPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StreamPage container!',
  },
});
