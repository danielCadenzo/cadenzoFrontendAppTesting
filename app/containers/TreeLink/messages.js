/*
 * TreeLink Messages
 *
 * This contains all the text for the TreeLink container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TreeLink';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TreeLink container!',
  },
});
