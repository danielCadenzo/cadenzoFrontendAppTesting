/*
 * ReviewPage Messages
 *
 * This contains all the text for the ReviewPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ReviewPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ReviewPage container!',
  },
});
