// import { selectVenueBookingPageDomain } from '../selectors';

describe('selectVenueBookingPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
