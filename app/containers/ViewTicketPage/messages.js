/*
 * ViewTicketPage Messages
 *
 * This contains all the text for the ViewTicketPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ViewTicketPage';

export default defineMessages({
  checkedIn: {
    id: `${scope}.checkedIn`,
    defaultMessage: "Checked in!!!! ✅ You're good to go! 😎",
  },
});
